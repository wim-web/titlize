{
  "name": "titlize",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.12",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/cli.ts --target=bun --outfile=dist/codex-title",
    "install:user": "bun src/install-user.ts",
    "uninstall:user": "bun src/install-user.ts --uninstall"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
